name: container

on:
  schedule:
    - cron: "0 3 * * 1" # Every Monday at 03:00 UTC
  workflow_dispatch:
  pull_request:
    paths:
      - "build/**"
      - "test/**"
      - "tasks/**"
      - ".github/workflows/container.yaml"
  push:
    branches:
      - main
    paths:
      - "build/**"
      - "test/**"
      - "tasks/**"
      - ".github/workflows/container.yaml"
    tags:
      - "*"

permissions:
  packages: write

jobs:
  release:
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        distro: [fedora]

    env:
      DESTINATION_REGISTRY: ghcr.io
      IMAGE_NAMESPACE: ${{ github.repository_owner }}
      FEDORA_VERSION: "43"
      UBUNTU_VERSION: "24.04"
      TOOLS_REGISTRY: ghcr.io/tankdonut

    steps:
      - name: Free disk space on runner
        run: |
          sudo rm -rf /usr/share/dotnet || true
          sudo rm -rf /usr/local/lib/android || true
          sudo rm -rf /opt/ghc || true
          sudo rm -rf /opt/hostedtoolcache/CodeQL || true
          sudo rm -rf "$AGENT_TOOLSDIRECTORY" || true

          sudo docker system prune -af || true
          sudo docker image prune -af || true
          sudo docker volume prune -f || true

          sudo rm -rf /tmp/* /var/tmp/* || true

      - name: Checkout repository
        uses: actions/checkout@v6
        with:
          submodules: recursive

      - name: Install uv
        run: |
          curl -LsSf https://astral.sh/uv/install.sh | sh
          echo "$HOME/.local/bin" >> $GITHUB_PATH

      - name: Create virtual environment via uv
        run: |
          uv venv

      - name: Install Python dependencies via uv
        run: |
          uv pip install invoke python-dotenv

      - name: Install Podman
        run: |
          sudo apt-get update
          sudo apt-get install -y podman

      - name: Login to GHCR
        run: |
          echo "${{ secrets.GITHUB_TOKEN }}" | \
            podman login ghcr.io \
              --username "${{ github.actor }}" \
              --password-stdin

      - name: Run release pipeline
        run: |
          if [ "${{ matrix.distro }}" = "fedora" ]; then
            uv run inv build.release-fedora
          else
            uv run inv build.release-ubuntu
          fi
